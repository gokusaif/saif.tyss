import { FilterPartnerPipe } from './filter-partner.pipe';

describe('FilterPartnerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPartnerPipe();
    expect(pipe).toBeTruthy();
  });
});
